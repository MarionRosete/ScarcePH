export interface ApiErrorResponse {
  message?: string;
}

export type VariationParams = {
  condition: string,
  price: number,
  size: string,
  image: string,
  status: string,
  stock: number,
  url:string,
}

